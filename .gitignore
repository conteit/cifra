# Dependencies
node_modules/

# Build output
build/
storybook-static/
.react-router/

# Testing
test-results/
playwright-report/
coverage/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.iml

# Logs
*.log

# Firebase emulator scratch. `firebase.json` is committed (Auth emulator only);
# everything the emulator writes beside it is per-run state.
.firebase/
firebase-debug.*.log
firestore-debug.log
ui-debug.log

# SDD scratch (not part of the app)
.superpowers/

*storybook.log
.vercel
.env*
!.env.example

# Agent worktrees (each is a full checkout carrying its own biome.json, which
# Biome 2 rejects as a nested root config — see issue #40)
.claude/
